
#pragma once
class CEnum {
public:
	//Globally or widely used ENUMs
	enum direction { UP, DOWN, LEFT, RIGHT, DIRECTION_NONE};
	enum soundExtension { WAV, MP4 };
	enum buttonName { BUTTON_NONE, STARTGAME, QUIT, HEALTHPACK, MUTE};
};